[package]
name = "ddnet-engine-server"
version = "0.0.1"
edition = "2021"
license = "Zlib"

[lib]
path = "lib.rs"
crate-type = ["rlib", "staticlib"]

[dependencies]
# C++ bindings for DDNet
cxx = "1.0"

# Supported database backends
rusqlite = "0.32.1"
mysql = "25.0.1"
